name: Sync Partner Integration Settings API
on:
  push:
    branches:
      - main
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: readmeio/rdme@6.5.0
        with:
          rdme: openapi ./docs/apis/partnerIntSet/partner_integration_settings_api.yaml --key=${{ secrets.README_API_KEY }} --id=615eb7b60319170356179796
